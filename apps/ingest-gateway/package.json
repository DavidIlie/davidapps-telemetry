{
  "name": "@davidapps/telemetry-gateway",
  "version": "0.0.0",
  "private": true,
  "description": "Stateless public telemetry ingress guard for Grafana Alloy.",
  "license": "Apache-2.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsdown src/index.ts src/server.ts --format esm --dts",
    "check": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json",
    "clean": "rm -rf dist coverage",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "fastify": "5.12.0"
  }
}

