# `@davidapps/telemetry-core`

Runtime-neutral telemetry client, signal types, redaction, and adapter contracts. This package performs no network requests by itself; install a platform adapter or provide a `TelemetryAdapter` implementation.

